From a0f6e8a5ba9140f7a78ed1796b6ae33fcf5a0144 Mon Sep 17 00:00:00 2001
From: Patryk Kups <pkups@anaconda.com>
Date: Thu, 24 Jul 2025 19:40:57 +0200
Subject: [PATCH] patch

---
 scripts/build.py | 30 ++++++++++++++++++++++++------
 1 file changed, 24 insertions(+), 6 deletions(-)

diff --git a/scripts/build.py b/scripts/build.py
index 58ec45b..193cf47 100644
--- a/scripts/build.py
+++ b/scripts/build.py
@@ -34,7 +34,7 @@ def detect_arch():
             and ("libc" not in arch or arch.get("libc") == libc)
         ):
             if arch["libdir"]:
-                arch["libdir"] = os.path.expanduser(arch["libdir"])
+                arch["libdir"] = os.environ["LIBDIR_PATH"]
             else:
                 global tmpdir
                 if "CI" in os.environ:
@@ -42,8 +42,8 @@ def detect_arch():
                     os.makedirs(tmpdir, exist_ok=True)
                     arch["libdir"] = tmpdir
                 else:
-                    tmpdir = tempfile.TemporaryDirectory()
-                    arch["libdir"] = tmpdir.name
+                    # tmpdir = tempfile.TemporaryDirectory()
+                    arch["libdir"] = os.environ["LIBDIR_PATH"]
             return arch
     raise Exception(f"Unsupported arch: {uname}")
 
@@ -138,8 +138,26 @@ def get_curl_libraries():
 ffibuilder = FFI()
 system = platform.system()
 root_dir = Path(__file__).parent.parent
-download_libcurl()
-
+# download_libcurl()
+
+# The following code is part of the download_libcurl() function.
+# It ensures that all binaries are placed directly in the libdir, instead of subdirectories like libdir/lib or libdir/bin.
+# This ensures that the Windows linker has everything it needs in a single directory: arch["libdir"]
+if arch["system"] == "Windows":
+    for file in glob(os.path.join(arch["libdir"], "lib/*.lib")):
+        shutil.move(file, arch["libdir"])
+    for file in glob(os.path.join(arch["libdir"], "bin/*.dll")):
+        shutil.move(file, arch["libdir"])
+
+extra_link_args = []
+
+if arch["system"] == "Darwin":
+    extra_link_args.extend([
+        f"-Wl,-rpath,{os.environ['RPATH_DIR']}",
+        "-lstdc++"
+    ])
+elif arch["system"] != "Windows":
+    extra_link_args.append("-lstdc++")
 
 ffibuilder.set_source(
     "curl_cffi._wrapper",
@@ -161,7 +179,7 @@ ffibuilder.set_source(
     extra_compile_args=(
         ["-Wno-implicit-function-declaration"] if system == "Darwin" else []
     ),
-    extra_link_args=(["-lstdc++"] if system != "Windows" else []),
+    extra_link_args=extra_link_args,
 )
 
 with open(root_dir / "ffi/cdef.c") as f:
-- 
2.39.5 (Apple Git-154)

